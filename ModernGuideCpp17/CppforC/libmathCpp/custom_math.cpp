#include "custom_math.h"

int CustomMath::custom_add(int x, int y)
{
    return x = y;
}

int CustomMath::custom_sub(int x, int y)
{
    return x - y;
}

int CustomMath::custom_mul(int x, int y)
{
    return x * y;
}

int CustomMath::custom_div(int x, int y)
{
    return x / y;
}
