#include "custom_math.h"

int __callback__ custom_add(int x, int y)
{
    return x + y;
}

int __callback__ custom_sub(int x, int y)
{
    return x - y;
}

int __callback__ custom_mul(int x, int y)
{
    return x * y;
}

int __callback__ custom_div(int x, int y)
{

    return x / y;
}
