# Compile Command for 05_initializer_list

## Command line with g++ or clang++

```shell
g++ initializer_list.cpp -std=c++2a -o initializer_list

clang++ initializer_list.cpp -std=c++2a -o initializer_list

```
