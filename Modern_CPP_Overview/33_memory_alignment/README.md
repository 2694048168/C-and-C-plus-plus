# Compile Command for 33_memory_alignment

## Command line with g++ or clang++

```shell
g++ memory_alignment.cpp -std=c++2a -Wall -o memory_alignment

clang++ memory_alignment.cpp -std=c++2a -Wall -o memory_alignment
```
