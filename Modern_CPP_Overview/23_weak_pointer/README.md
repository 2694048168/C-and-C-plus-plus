# Compile Command for 23_weak_pointer

## Command line with g++ or clang++

```shell
g++ weak_pointer.cpp -std=c++2a -Wall -o weak_pointer

clang++ weak_pointer.cpp -std=c++2a -Wall -o weak_pointer
```
