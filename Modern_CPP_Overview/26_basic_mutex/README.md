# Compile Command for 26_basic_mutex

## Command line with g++ or clang++

```shell
g++ basic_mutex.cpp -std=c++2a -Wall -o basic_mutex

clang++ basic_mutex.cpp -std=c++2a -Wall -o basic_mutex -pthread
```
