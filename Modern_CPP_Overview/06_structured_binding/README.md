# Compile Command for 06_structured_binding

## Command line with g++ or clang++

```shell
g++ structuredd_binding.cpp -std=c++2a -o structuredd_binding

clang++ structuredd_binding.cpp -std=c++2a -o structuredd_binding
```
