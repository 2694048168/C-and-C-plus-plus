# Compile Command for 10_magic_templates

## Command line with g++ or clang++

```shell
g++ magic_template.cpp -std=c++2a -Wall -o magic_template

clang++ magic_template.cpp -std=c++2a -Wall -o magic_template
```
