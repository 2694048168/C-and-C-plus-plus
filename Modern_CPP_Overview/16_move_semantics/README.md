# Compile Command for 16_move_semantics

## Command line with g++ or clang++

```shell
g++ move_semantics.cpp -std=c++2a -Wall -o move_semantics

clang++ move_semantics.cpp -std=c++2a -Wall -o move_semantics
```
