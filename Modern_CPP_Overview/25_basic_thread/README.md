# Compile Command for 25_basic_thread

## Command line with g++ or clang++

```shell
g++ basic_thread.cpp -std=c++2a -Wall -o basic_thread

clang++ basic_thread.cpp -std=c++2a -Wall -o basic_thread
```
