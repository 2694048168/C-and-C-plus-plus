# Compile Command for 29_atomic_memory

## Command line with g++ or clang++

```shell
g++ atomic_memory.cpp -std=c++2a -Wall -o atomic_memory

clang++ atomic_memory.cpp -std=c++2a -Wall -o atomic_memory -pthread
```
