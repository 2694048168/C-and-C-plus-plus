# Compile Command for 13_lambda_expression

## Command line with g++ or clang++

```shell
g++ lambda_expression.cpp -std=c++2a -Wall -o lambda_expression

clang++ lambda_expression.cpp -std=c++2a -Wall -o lambda_expression
```
