# Compile Command for 22_unique_pointer

## Command line with g++ or clang++

```shell
g++ unique_pointer.cpp -std=c++2a -Wall -o unique_pointer

clang++ unique_pointer.cpp -std=c++2a -Wall -o unique_pointer
```
