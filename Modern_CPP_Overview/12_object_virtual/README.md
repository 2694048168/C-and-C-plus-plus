# Compile Command for 12_object_virtual

## Command line with g++ or clang++

```shell
g++ object_virtual.cpp -std=c++2a -Wall -o object_virtual

clang++ object_virtual.cpp -std=c++2a -Wall -o object_virtual
```
